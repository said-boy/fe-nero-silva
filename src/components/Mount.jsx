import mount from "@/assets/mount.png";

export default function Mount() {
  return (
    <div className="flex content-end overflow-hidden" style={{ height: "100vh" }}>
      <img src={mount} alt="Mount" style={{ width: "100%" }} />
    </div>
  );
}
